# 2026-03-08

- Codex-orchestrator now defaults future real swarms/delegated PM work to a shared `STATE.yaml` coordination model backed by `coordination/PROJECT_REGISTRY.yaml`.
- `scripts/codex-batch.py init` is the default bootstrap for a new tracked project: it creates a batch manifest, project `STATE.yaml`, and a registry entry together.
- `scripts/codex-lane-launch.sh` now auto-attaches lane manifests to a batch state file when available and syncs task status on start/finish.
- `scripts/codex-lane-launch.sh` was hardened for truly empty repos: it now tolerates repos without a valid `HEAD` when capturing branch/head metadata before lane launch.
- New standing execution policy from Stephen: never use ACP thread execution for Codex work again; use only locally tracked Codex CLI lanes with logs/artifacts under `runs/` unless Stephen later explicitly reverses that policy.
- Active project thread: `verify-patch` was bootstrapped as a tracked batch project with phased Codex lanes. Phase 1 (`4a13bdf`) and Phase 2 (`532a1d0`) were committed and pushed to both GitHub and Codeberg. Phase 3 completed locally as commit `ce54e3a` (`Implement Phase 3 baseline attribution and GitHub rendering`) and left `~/github/verify-patch` clean but still ahead of remotes by one commit at the time of memory flush.
- `verify-patch` project state file: `/Users/sawyer/.openclaw/workspace-codex-orchestrator/runs/batches/20260308T163901Z-verify-patch/STATE.yaml`. At flush time the repo reality is ahead of the shared state bookkeeping: Phase 3 is locally committed, but the background lane had not fully closed out its final writeback yet.
- Active project thread: `abi-audit` was bootstrapped as a tracked batch project with shared coordination state at `/Users/sawyer/.openclaw/workspace-codex-orchestrator/runs/batches/20260308T162204Z-abi-audit/STATE.yaml`.
- `abi-audit` phase status at flush time: Phase 1 bootstrap committed and pushed as `999e6e1` (`feat: bootstrap abi audit MVP`); Phase 2 committed locally as `d70419d` (`feat: deepen abi discovery and snapshot coverage`) on `codex/phase-2-core-discovery`; Phase 3 (`codex/phase-3-binary-header-audit`) is in progress with no commit yet.
- `abi-audit` implementation is explicitly running via local Codex CLI lanes with artifacts under `runs/`, not ACP threads. Relevant run dirs: `runs/20260308T162406Z-abi-audit-bootstrap`, `runs/20260308T170215Z-abi-audit-phase-2-core-discovery`, and `runs/20260308T173632Z-abi-audit-phase-3-binary-header-audit`.
- `abi-audit` repo/worktree layout at flush time: canonical repo `/Users/sawyer/github/abi-audit`; phase worktrees under `/Users/sawyer/.openclaw/worktrees/abi-audit/` to keep implementation lanes isolated.
- Phase 3 design direction for `abi-audit`: add artifact evidence to snapshots, add explicit optional header-sync config/workflow, and cross-check source exports vs headers vs built artifacts without overstating binary ABI guarantees. `cbindgen` is not installed locally, so header-sync support must remain optional/evidence-based.
- Active project thread: `cargo-trust` was bootstrapped as a tracked batch project with shared state at `/Users/sawyer/.openclaw/workspace-codex-orchestrator/runs/batches/20260308T162615Z-cargo-trust/STATE.yaml` and has been executed entirely via local Codex CLI lanes under `runs/`, not ACP threads.
- `cargo-trust` milestones pushed to both GitHub and Codeberg so far: `882a814` (`chore: initialize repository`), `914a2a0` (`feat: bootstrap cargo-trust foundation`), `346754c` (`feat: add external dependency signals`), `baf61b7` (`feat: add ci gating and reporting outputs`), `c655e39` (`feat: import cargo-vet audit evidence`), and `06a4875` (`feat: import cargo-deny policy evidence`).
- `cargo-trust` phase 6 automation-surface work is in progress in `runs/20260308T193347Z-cargo-trust-automation-surface`; the dirty tree includes a new `.github/workflows/ci.yml`, README/BUILD updates, and a new `render` automation path in `crates/cargo-trust/src/main.rs` plus smoke-test additions.
- During phase 6 intervention, multiple stale `cargo-trust` helper/test processes and a competing rust-analyzer `cargo check --workspace` against `~/github/cargo-trust` were found contending for the same target directory; all were killed before retrying verification. A local patch was also applied to `crates/cargo-trust/tests/smoke.rs` so CLI test helpers set `CARGO_TRUST_OFFLINE=1` for deterministic automation-path tests.
- cargo-advisor progress: phase 1 committed/pushed as `7048235`, phase 2 as `a875b3e`, phase 3 as `ae4c03e`; phase 4 release/publish polish was being committed/pushed next.
- Important local environment issue discovered during cargo-advisor phase 4 on Stephen's MacBook Air: freshly compiled binaries (including trivial Rust binaries, Cargo build scripts, and cargo-advisor test binaries) began hanging at `_dyld_start`, blocking local `cargo test`, `cargo check`, `cargo clippy`, and direct execution despite earlier phases verifying successfully. This looks like a macOS/dyld/system-state problem, not a repo logic regression.
- Release posture for cargo-advisor: proceed by trusting earlier phase verification plus CI/GitHub verification when the local dyld issue blocks fresh local binary execution; call out the local verification gap explicitly rather than pretending checks passed.
