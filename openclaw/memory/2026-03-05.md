# 2026-03-05

## Session: Dev Environment Audit & Optimization

### Model override fix
- Cleared stale per-session model override that was persisting in runtime metadata. Used `session_status model=default` to clear.

### macOS dev setup — comprehensive audit + install

**Brew installs (9 new):**
- aria2, watchman, difftastic, mkcert, entr, parallel, pv, nmap, mosh

**Python AI/ML stack installed:**
- PyTorch 2.10.0 (MPS: ✓), torchvision, torchaudio
- transformers 5.3.0, diffusers 0.37.0, accelerate, safetensors
- gradio, datasets, Pillow, scipy, matplotlib, tiktoken, einops, sentencepiece
- All via pip3 --break-system-packages (matching existing MLX install)

**Ollama:** qwen2.5:14b pulled (9GB) — solid general-purpose local LLM for 32GB RAM

**Git config additions:**
- diff.algorithm=histogram, rebase.autosquash/autostash, column.ui=auto
- branch.sort=-committerdate, tag.sort=-version:refname, transfer.fsckobjects=true
- core.autocrlf=input, core.fsmonitor=true, core.untrackedcache=true
- delta: dark=true, hyperlinks=true

**Zshrc improvements:**
- Added LANG/LC_ALL locale, HOMEBREW_NO_ANALYTICS, PYTORCH_MPS_HIGH_WATERMARK_RATIO
- Docker aliases (dc, dps, dclean), network aliases (ports, myip, localip)
- diff alias → difft, weather, path helpers
- Functions: killport, clone, extract (universal), bench (hyperfine wrapper)
- Tailscale CLI path

**Starship config enhanced:**
- Added bun, java, lua, zig, elixir, php, kubernetes modules
- Added jobs indicator

**Manual action needed:**
- Run `mkcert -install` in terminal (needs sudo for CA trust)

### Full documentation audit & fix

**Issues found and fixed:**
1. **Grimoire AGENTS.md drift** — root + openclaw/ copies had stale model references. Synced from workspace canonical.
2. **MEMORY.md stale ACP default** — said `default **claude**`, config is actually `codex`. Fixed.
3. **MEMORY.md Luma decision** — said "Opus 4.6 primary", now correctly notes it follows global policy (Opus 4.6 primary, Codex 5.3 fallback).
4. **Two cron jobs validating stale model** — `agent-bench-weekly-smoke` and `weekly-agent-bench-review` had stale model checks. Updated to Opus 4.6 primary + Codex 5.3 fallback.
5. **Daily bench health check** — didn't include `contributor` or `luma` in expected agents list. Added.
6. **Both weekly cron scopes** — didn't include `contributor` or `luma`. Added.
7. **TOOLS.md** — updated integration date, expanded CLI inventory, added Python AI/ML stack section, added Ollama.
8. **MEMORY.md repos** — added `dotfiles`, `dunamismax` (profile), `forks/openclaw` which exist on disk but weren't documented.
9. **All grimoire copies verified in sync** — SOUL, AGENTS, MEMORY, TOOLS, CONTRIBUTING all match workspace canonical.
10. Committed + pushed to both GitHub and Codeberg.

### Specialist agent bench cleanup

**Deleted 6 redundant agents** (zero sessions across all, never used for real work):
- samantha (coding — redundant with PTY background agents)
- shipwright (release/CI — no complex pipelines)
- caretaker (repo hygiene — Scry handles inline)
- archivist (docs/memory — Scry handles directly)
- scout (research — Scry has web_search/web_fetch)
- operator (infra — Scry handles with exec/nodes)

**Cleanup performed:**
- Removed 6 weekly smoke cron jobs
- Removed agents from config (agents.list)
- Updated main + openclaw-maintainer allowAgents
- Deleted workspace dirs + agent dirs for all 6
- Updated daily bench healthcheck, weekly smoke, and weekly optimization crons to reference only surviving agents
- Updated AGENTS.md bench roster

**Remaining bench (7 total):** main, sentinel, reviewer (Arbiter), builder-mobile (Pixel), openclaw-maintainer (Keeper), contributor (Anvil), luma
