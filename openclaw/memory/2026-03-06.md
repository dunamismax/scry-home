# 2026-03-06 (Friday)

## Session Log

- 08:17 — Fresh session started. Opus 4.6, Signal DM.
- 08:38 — Stephen requested PR #36590 review + GPT-5.4 upgrade.
- 08:40 — Pulled latest OpenClaw main (fast-forward, 666 files changed). PR #36590 (GPT-5.4 forward-compat + Codex OAuth) confirmed merged. Fork synced to latest main, force-pushed.
- 08:41 — Applied config.patch: switched primary model from `anthropic/claude-opus-4-6` to `openai-codex/gpt-5.4` for all agents (main + 7 specialists). Opus 4.6 now fallback only. Added `gpt54` alias. Gateway restarted with new code + config.
- 08:42 — Updated workspace docs (MEMORY.md, AGENTS.md, TOOLS.md) to reflect model switch.

- 08:57 — Identified the first root cause of the phantom "running on Opus" bug: session store entries (`~/.openclaw/agents/main/sessions/sessions.json`) had stale `model: "claude-opus-4-6"` / `modelProvider: "anthropic"` fields baked in from when Opus was the default. These fields take priority over config defaults in `resolveSessionModelRef()` and survive `/reset`. Cleared stale entries across main + cron sessions as an immediate mitigation.
- 09:20 — Confirmed cleanup alone was insufficient because `sessions.reset` persisted the active runtime model back into the new session entry. Patched OpenClaw `src/gateway/server-methods/sessions.ts` to stop copying `model` / `modelProvider` on reset, added regression coverage, rebuilt the live gateway from `~/openclaw`, and verified new sessions stay on `openai-codex/gpt-5.4` without the false Opus greeting.

## Key Decisions

- GPT-5.4 is now the primary model everywhere. Opus 4.6 is fallback only. This is a Stephen directive, not a Scry recommendation.
- Auth unchanged — `openai-codex:default` OAuth covers GPT-5.4 via the new forward-compat layer from PR #36590.
- Background Codex CLI work now routes through `codex-orchestrator` by default. Main and non-Codex specialists should not use ACP `agentId:"codex"` or direct Codex launches for repo implementation work.
- Codex orchestrator protocol tightened: it now owes proactive launch/milestone/heartbeat/blocker/completion updates upstream when transcript visibility is limited.
