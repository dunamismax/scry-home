# 2026-03-07

- Consolidated the standalone `dotfiles` repo into `scry-home` under `workstation/` so tracked workstation snapshots now live alongside Scry's backup tooling.
- Kept plaintext workstation snapshots in `workstation/`, backup automation in `scripts/ops/`, and encrypted restore material in `vault/`.
- Cleaned the repo's local naming/path defaults so scripts and docs resolve the checkout as `scry-home` instead of `grimoire`.
