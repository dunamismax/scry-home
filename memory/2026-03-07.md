# 2026-03-07

- Consolidated the standalone `dotfiles` repo into `grimoire` under `workstation/` so tracked workstation snapshots now live alongside Grimoire's backup tooling.
- Kept plaintext workstation snapshots in `workstation/`, backup automation in `scripts/ops/`, and encrypted restore material in `vault/`.
